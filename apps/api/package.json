{
  "name": "@proto/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun run src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@proto/shared-types": "*",
    "viem": "^2.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
